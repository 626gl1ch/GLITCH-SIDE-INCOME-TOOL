name = "discovery-agent"
main = "src/index.ts"
compatibility_date = "2024-03-20"

[triggers]
crons = ["0 5 * * *"] # Run daily at 5 AM (WAT is UTC+1, so this is 6 AM WAT)

[vars]
# Add these via `wrangler secret put` later:
# SUPABASE_URL = ""
# SUPABASE_ANON_KEY = ""
# GEMINI_API_KEY = ""
# TELEGRAM_BOT_TOKEN = ""
# TELEGRAM_CHAT_ID = ""
